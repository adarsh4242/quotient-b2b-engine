import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { ArrowRight } from 'lucide-react';
import { useAuth } from '../context/AuthContext';

export default function Register() { const { register } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const submit = async (e) => { e.preventDefault(); try { await register(form); navigate('/dashboard'); } catch (err) { setError(err.response?.data?.message || 'Unable to create account'); } }; return <main className="auth-shell"><div className="auth-panel"><Link to="/login" className="text-sm font-bold text-slate-500">← Back to sign in</Link><p className="eyebrow mt-16">Create your workspace</p><h1 className="mt-3 font-display text-4xl font-bold text-ink">Start negotiating better.</h1><form onSubmit={submit} className="mt-10 space-y-5"><div><label className="label">Full name</label><input className="input" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} required /></div><div><label className="label">Work email</label><input className="input" type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} required /></div><div><label className="label">Password</label><input className="input" type="password" minLength="6" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} required /></div>{error && <p className="text-sm font-semibold text-red-600">{error}</p>}<button className="flex w-full items-center justify-center gap-2 bg-coral px-5 py-3.5 font-bold text-white">Create buyer account <ArrowRight size={17} /></button></form></div><div className="auth-visual register-visual"><p className="eyebrow text-mint/70">The buyer advantage</p><p className="mt-4 max-w-md font-display text-5xl font-bold leading-tight text-white">Less back and forth. More momentum.</p></div></main>; }