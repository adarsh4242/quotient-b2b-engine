import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { ArrowRight, PackageSearch } from 'lucide-react';
import { useAuth } from '../context/AuthContext';

export default function Login() { const { login } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ email: '', password: '' }); const [error, setError] = useState('');
  const submit = async (e) => { e.preventDefault(); try { await login(form); navigate('/dashboard'); } catch (err) { setError(err.response?.data?.message || 'Unable to sign in'); } };
  return <main className="auth-shell"><div className="auth-panel"><div className="mb-14 flex items-center gap-3"><span className="grid h-9 w-9 place-items-center bg-ink text-mint"><PackageSearch size={20} /></span><span className="font-display text-xl font-bold">quotient<span className="text-coral">.</span></span></div><p className="eyebrow">Buyer workspace</p><h1 className="mt-3 font-display text-4xl font-bold text-ink">Make the right deal.</h1><p className="mt-3 max-w-sm text-slate-500">Submit a target, see the math, and move from quote to purchase order in minutes.</p><form onSubmit={submit} className="mt-10 space-y-5"><div><label className="label">Email address</label><input className="input" type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} required /></div><div><label className="label">Password</label><input className="input" type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} required /></div>{error && <p className="text-sm font-semibold text-red-600">{error}</p>}<button className="flex w-full items-center justify-center gap-2 bg-ink px-5 py-3.5 font-bold text-white">Enter workspace <ArrowRight size={17} /></button></form><p className="mt-8 text-center text-sm text-slate-500">New to quotient? <Link to="/register" className="font-bold text-ink underline">Create an account</Link></p></div><div className="auth-visual"><div className="max-w-md"><p className="eyebrow text-mint/70">Dynamic pricing intelligence</p><p className="mt-4 font-display text-5xl font-bold leading-tight text-white">Every quote has a smarter next move.</p></div></div></main>; }