import { Check, Clock3, FileText, HandCoins, X } from 'lucide-react';

const state = { approved: { label: 'Approved', icon: Check, className: 'bg-mint text-emerald-800' }, countered: { label: 'Counter offer', icon: HandCoins, className: 'bg-orange-100 text-orange-800' }, pending: { label: 'Under review', icon: Clock3, className: 'bg-slate-100 text-slate-700' }, rejected: { label: 'Rejected', icon: X, className: 'bg-red-100 text-red-800' }, expired: { label: 'Expired', icon: Clock3, className: 'bg-slate-200 text-slate-700' } };
export default function QuoteStatusCard({ quote, onAccept }) {
  const config = state[quote.status] || state.pending; const Icon = config.icon; const unitPrice = quote.counterUnitPrice || quote.offeredUnitPrice;
  return <article className="border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-slate-400">Quote {String(quote._id).slice(-6)}</p><h3 className="mt-1 font-display text-lg font-bold text-ink">{quote.productId?.name || 'Product'}</h3></div><span className={`inline-flex items-center gap-1.5 px-2.5 py-1 text-xs font-bold ${config.className}`}><Icon size={14} />{config.label}</span></div><div className="mt-5 grid grid-cols-3 gap-3 border-y border-slate-100 py-4 text-sm"><div><p className="text-slate-400">Quantity</p><p className="mt-1 font-bold text-ink">{quote.requestedQty}</p></div><div><p className="text-slate-400">Unit price</p><p className="mt-1 font-bold text-ink">${unitPrice?.toFixed(2)}</p></div><div><p className="text-slate-400">Subtotal</p><p className="mt-1 font-bold text-ink">${(unitPrice * quote.requestedQty).toFixed(2)}</p></div></div>{quote.status === 'countered' && <button onClick={() => onAccept(quote)} className="mt-4 flex w-full items-center justify-center gap-2 bg-ink px-4 py-3 text-sm font-bold text-white transition hover:bg-coral"><FileText size={16} />Accept counter & generate PO</button>}</article>;
}